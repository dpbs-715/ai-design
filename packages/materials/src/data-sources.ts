/**
 * 内置静态数据源 id。物料模板的 `dataId` 引用这些常量,
 * web 端的默认数据源工厂也从这里取值,保证两边一致。
 */
export const DEFAULT_STATIC_OPTIONS_SOURCE_ID = 'default-static-options'

export const DEFAULT_STATIC_FORM_SOURCE_ID = 'default-static-form'
