{
  "name": "ai-design-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@ai-design/web",
    "dev:server": "turbo run dev --filter=@ai-design/server",
    "infra:up": "docker compose --env-file env/.env -f infra/compose.yaml up -d",
    "infra:down": "docker compose --env-file env/.env -f infra/compose.yaml down",
    "infra:status": "docker compose --env-file env/.env -f infra/compose.yaml ps",
    "infra:logs": "docker compose --env-file env/.env -f infra/compose.yaml logs -f",
    "db:migrate": "docker compose --env-file env/.env -f infra/compose.yaml exec -T postgres sh /opt/ai-design/postgres/migrate.sh",
    "build": "turbo run build",
    "type-check": "turbo run type-check",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.10.7"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  }
}
